<%- contentFor('body') %>
<section class="contact-page py-5">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-md-6 mb-4 mb-md-0">
        <h1 class="mb-4">Контакты</h1>
        <ul class="contact-list mb-4">
          <li><strong>Телефон:</strong> <a href="tel:+7xxxxxxxxxx">+7 (xxx) xxx xx xx</a></li>
          <li><strong>Email:</strong> <a href="mailto:info@lesnoy.ru">info@lesnoy.ru</a></li>
          <li><strong>Адрес:</strong> г. Оренбург, ул. Чкалова, 11</li>
        </ul>
        <p>Вы также можете написать нам через форму обратной связи:</p>
        <% if (locals.success && success.length) { %>
          <div class="alert alert-success"><%= success %></div>
        <% } %>
        <% if (locals.error && error.length) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <form class="contact-form mt-3" action="/contact" method="POST">
          <div class="mb-3">
            <input type="text" class="form-control" name="name" placeholder="Ваше имя" required>
          </div>
          <div class="mb-3">
            <input type="email" class="form-control" name="email" placeholder="Ваш email" required>
          </div>
          <div class="mb-3">
            <textarea class="form-control" name="message" rows="4" placeholder="Сообщение" required></textarea>
          </div>
          <button type="submit" class="btn btn-primary w-100">Отправить</button>
        </form>
        <p class="mt-2 text-muted" style="font-size:0.9rem">Нажимая на кнопку «Отправить», Вы соглашаетесь на обработку персональных данных</p>
      </div>
      <div class="col-md-6">
        <img src="/images/Главная.png" alt="Woodoren контакты" class="img-fluid rounded shadow">
      </div>
    </div>
  </div>
</section> 